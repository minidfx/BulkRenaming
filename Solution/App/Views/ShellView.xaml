<Page x:Class="App.Views.ShellView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:behaviors="using:App.Behaviors"
      xmlns:controls="using:WinRTXamlToolkit.Controls"
      mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding FolderSelected}"
                       VerticalAlignment="Center" />

            <Button Grid.Column="1" x:Name="BrowseAsync">
                <SymbolIcon Symbol="OpenLocal" />
            </Button>
        </Grid>

        <Border Grid.Row="1" BorderThickness="0 1 0 1" BorderBrush="{ThemeResource TextBoxBorderThemeBrush}">
            <ListView ItemsSource="{Binding Files}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <RichTextBlock behaviors:RegexColorBehavior.FileName="{Binding Name}"
                                           behaviors:RegexColorBehavior.Result="{Binding RegexResult.Result}"
                                           behaviors:RegexColorBehavior.FuturResult="{Binding RegexResult.FuturResult}" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" Grid.Column="0" Margin="10">
                <controls:WatermarkTextBox WatermarkText="Pattern" 
                                           Text="{Binding Pattern.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                           Margin="0 0 0 10"/>
                <controls:WatermarkTextBox WatermarkText="Replace pattern"
                                           Text="{Binding ReplacePattern.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <Button Grid.Column="1" x:Name="ApplyAsync" Margin="0 0 10 0">
                <SymbolIcon Symbol="Accept" />
            </Button>
        </Grid>

        <controls:CustomAppBar Grid.Row="3" />
    </Grid>

</Page>